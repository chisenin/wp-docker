# WordPress Docker环境变量配置模板
# 请复制此文件为.env并根据实际环境修改配置

# Docker相关配置
DOCKERHUB_USERNAME=your_dockerhub_username  # Docker Hub用户名
PHP_VERSION=8.3.26  # PHP版本
NGINX_VERSION=1.27.2  # Nginx版本

# 数据库配置
MYSQL_ROOT_PASSWORD=your_root_password  # MySQL root用户密码
MYSQL_DATABASE=wordpress  # WordPress数据库名称
MYSQL_USER=wordpress  # WordPress数据库用户
MYSQL_PASSWORD=your_wordpress_password  # WordPress数据库用户密码

# WordPress配置
WORDPRESS_DB_HOST=mariadb:3306  # 数据库主机（与docker-compose.yml中的服务名保持一致）
WORDPRESS_DB_USER=${MYSQL_USER}  # WordPress数据库用户
WORDPRESS_DB_PASSWORD=${MYSQL_PASSWORD}  # WordPress数据库密码
WORDPRESS_DB_NAME=${MYSQL_DATABASE}  # WordPress数据库名称
WORDPRESS_TABLE_PREFIX=wp_  # WordPress数据库表前缀

# Redis配置
REDIS_HOST=redis  # Redis主机
REDIS_PORT=6379  # Redis端口
REDIS_PASSWORD=your_redis_password  # Redis认证密码（可选，生产环境建议设置）
REDIS_MAXMEMORY=256mb  # Redis最大内存限制

# 可选配置（生产环境使用）
PHP_MEMORY_LIMIT=512M  # PHP内存限制
UPLOAD_MAX_FILESIZE=64M  # 最大上传文件大小
USE_CN_MIRROR=false  # 是否使用中国镜像源
# 资源限制
CPU_LIMIT=2
MEMORY_LIMIT=2048m

# WordPress安全密钥配置（必须在生产环境中设置为随机值）
# 可以使用https://api.wordpress.org/secret-key/1.1/salt/生成
WORDPRESS_AUTH_KEY=put-your-unique-phrase-here
WORDPRESS_SECURE_AUTH_KEY=put-your-unique-phrase-here
WORDPRESS_LOGGED_IN_KEY=put-your-unique-phrase-here
WORDPRESS_NONCE_KEY=put-your-unique-phrase-here
WORDPRESS_AUTH_SALT=put-your-unique-phrase-here
WORDPRESS_SECURE_AUTH_SALT=put-your-unique-phrase-here
WORDPRESS_LOGGED_IN_SALT=put-your-unique-phrase-here
WORDPRESS_NONCE_SALT=put-your-unique-phrase-here